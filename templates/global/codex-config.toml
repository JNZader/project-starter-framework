# Codex CLI Configuration
# https://developers.openai.com/codex/config-reference/

# Model: gpt-5.3-codex | gpt-5.2-codex | gpt-5.1-codex-max | gpt-5.1-codex-mini
model = "gpt-5.3-codex"

# Approval policy: "on-request" | "on-failure" | "never" | "untrusted" | "reject"
approval_policy = "on-request"
