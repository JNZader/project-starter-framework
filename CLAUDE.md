# Claude Code Instructions

> Instrucciones para Claude Code en este proyecto

## IMPORTANTE

- Si un test o comando no da respuesta o la respuesta es vacía, asume que el comando falló y pide que lo ejecute manualmente
- **NO agregar atribuciones a IA** en commits, PRs, código o documentación
- **Nunca usar**: "Co-authored-by: Claude", "Made by AI", "Generated by Claude", etc.

## Proyecto

- **Nombre:** [NOMBRE_PROYECTO]
- **Stack:** [STACK]

## CI Local

Antes de push, ejecutar CI local:

```bash
./.ci-local/ci-local.sh full    # CI completo
./.ci-local/ci-local.sh quick   # Solo lint + compile
./.ci-local/ci-local.sh shell   # Debug en entorno CI
```

## Branching Strategy

```
main (producción)
  ↑ PR
develop (integración)
  ↑ PR
feature/descripcion (trabajo)
```

## Convenciones

### Commits (Conventional)

```
feat(scope): add feature
fix(scope): fix bug
docs(scope): update docs
refactor(scope): refactor code
test(scope): add tests
chore(scope): maintenance
```

### Branches

```
feature/descripcion
fix/descripcion
docs/descripcion
```

## Archivos en .gitignore

- CLAUDE.md está en .gitignore (no commitear)

## Comandos Frecuentes

```bash
# CI Local
./.ci-local/ci-local.sh quick   # Validación rápida
./.ci-local/ci-local.sh full    # CI completo

# [Agregar comandos específicos del proyecto]
```
